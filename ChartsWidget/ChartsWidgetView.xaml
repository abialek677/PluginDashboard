<UserControl x:Class="ChartsWidget.ChartsWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="300" Width="500">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Status / debug -->
        <TextBlock x:Name="StatusText" Grid.Row="0" Text="Status: oczekiwanie..." 
                   Margin="4" FontWeight="Bold" TextWrapping="Wrap"/>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <StackPanel Orientation="Horizontal" Margin="8" x:Name="BarsPanel" VerticalAlignment="Bottom"/>
        </ScrollViewer>
    </Grid>
</UserControl>