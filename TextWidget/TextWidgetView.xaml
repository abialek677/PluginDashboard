<UserControl x:Class="TextWidget.TextWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="400" Width="700">
    <StackPanel Margin="8">
        <TextBlock Text="Text widget" FontWeight="Bold" FontSize="14"/>
        <TextBlock Text="{Binding CharacterCount, StringFormat=Characters: {0}}" Margin="0,8,0,0"/>
        <TextBlock Text="{Binding WordCount, StringFormat=Words: {0}}" Margin="0,4,0,0"/>
        <TextBlock Text="{Binding Preview, StringFormat=Parsed string: `{0}`}" TextWrapping="Wrap" Margin="0,8,0,0"/>
    </StackPanel>
</UserControl>