<UserControl x:Class="TextWidget.TextWidgetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="200" Width="420">
    <StackPanel Margin="8">
        <TextBlock Text="Analizator Tekstu" FontWeight="Bold" FontSize="14"/>
        <TextBlock Text="{Binding CharacterCount, StringFormat=Znaków: {0}}" Margin="0,8,0,0"/>
        <TextBlock Text="{Binding WordCount, StringFormat=Słów: {0}}" Margin="0,4,0,0"/>
        <TextBlock Text="{Binding Preview, StringFormat=Podgląd: {0}}" TextWrapping="Wrap" Margin="0,8,0,0"/>
    </StackPanel>
</UserControl>